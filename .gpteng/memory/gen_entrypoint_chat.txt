```sh
# There are no dependencies to install for this codebase as it is a static HTML/JS application.
```

```sh
# Serve the current directory using a simple static server like python's built-in HTTP server.
# Python 2.x
python -m SimpleHTTPServer 8000 &

# Python 3.x
python3 -m http.server 8000 &
```